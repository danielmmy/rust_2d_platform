[package]
name = "wisp"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19"

# Playable speed in debug: optimise dependencies (Bevy) heavily, our code lightly.
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
